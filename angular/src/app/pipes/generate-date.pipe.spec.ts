import { GenerateDatePipe } from './generate-date.pipe';

describe('GenerateDatePipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateDatePipe();
    expect(pipe).toBeTruthy();
  });
});
